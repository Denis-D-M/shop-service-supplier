server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5455/supplierdb
    username: postgres
    password: 12341234
  jpa:
    hibernate:
      ddl-auto: create
      database-platform: org.hibernate.dialect.PostgreSQL9Dialect